---
title: Flows
api: 'POST flow/{uuid}/flow-execution'
---

# When to use this method

Choose Flows when you want to send messages to multiple recipients and expect a response or interaction from them. Flows are similar to chatbots, allowing for dynamic, two-way communication. Use this method to engage your users in conversations, answer queries, or gather information.

# Header Parameters
<ParamField header="Authorization" type="string" placeholder="Token <Your token>" required>The authorization token</ParamField>

# Path Parameters
<ParamField path="uuid" type="string" placeholder="08a1372c-16ef-40c9-9979-45e4cfcb37d1" required>UUID of the Flow to send</ParamField>

# Body Parameters
<ParamField body="execute_for" type="string" placeholder="08a1372c-16ef-40c9-9979-45e4cfcb37d1" required>UUID of the Flow to send</ParamField>
<ParamField body="has_priority" type="string" placeholder="08a1372c-16ef-40c9-9979-45e4cfcb37d1" required>UUID of the Flow to send</ParamField>
<ParamField body="contact_lists" type="string" placeholder="08a1372c-16ef-40c9-9979-45e4cfcb37d1" required>UUID of the Flow to send</ParamField>

<ResponseExample>
```json
{
  "id": 2,
  "flow": "a3ff7ee5-0c11-49e2-a0d6-7e316626f7b1",
  "status": "READY",
  "created_by": "erik@hilos.io",
  "created_on": "2023-03-15T11:00:00Z"
  // Additional fields
}
```
</ResponseExample>